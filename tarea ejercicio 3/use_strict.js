"use strict";

//El código "use strict" es una directiva en JavaScript que activa el modo estricto (strict mode). Este modo cambia la forma en que el motor de JavaScript interpreta el código, aplicando 
//un conjunto de reglas más estrictas que ayudan a evitar errores comunes y a escribir un código más seguro y eficiente. debe estár al principio de los scripts. Si no, el modo estricto 
//podría no estar activado. No hay ninguna directiva del tipo "no use strict" que haga al motor volver al comportamiento anterior.
//Una vez entramos en modo estricto, no hay vuelta atrás.